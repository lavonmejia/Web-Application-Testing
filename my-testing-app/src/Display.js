// import React from "react";
// import { useState } from "react";
// import Logic from './Logic';



// const Display = (props, manageBalls, manageFouls, manageHits, manageStrikes) => {
//     return (
//       <div>
//       <button onClick={() => manageStrikes()}>strikes:{props.strikes}</button>

//       <button onClick={() => manageBalls()}>balls: {props.balls}</button>

//       <button onClick={() => manageFouls()}>fouls: {props.fouls}</button>

//       <button onClick={() => manageHits()}>hits: {props.hits}</button>
//     </div>
//   );
// }

// export default Display;